# Modelo de datos — Versión 7

El front sigue SIN datos propios. Crece su consumo de la API (las 11
entidades + `rol-usuario/usuario/{email}`) y su sesión suma UNA clave:
`es_admin` (booleano que decide la API al entrar).
