# Guía IA — Versión 7 (front completo; estudiante :8166)

Entregue esta carpeta + su front v6 construido, con este encargo:

```
Construye la v7 de 2_spec.md sobre mi front v6: un registro de metadatos
(entidades.py) y rutas genéricas /e/<entidad> para las 11 entidades (los
puentes sin editar, con DELETE de pareja exacta), FKs como select
cargados de la API, y menú por roles (es_admin lo decide la API con
rol-usuario/usuario/{email}, rol 1). La API NO se toca. Fases de
8_tasks.md en orden, sin avanzar en rojo.
```

Si la IA valida negocio en el front o toca la API, deténgala. El cierre
(quickstart) lo corre usted.
