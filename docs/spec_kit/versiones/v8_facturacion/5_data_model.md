# Modelo de datos — Versión 8

El front sigue sin datos propios. Nuevo consumo: los 4 endpoints de
factura de la v2 (`GET /api/factura`, `GET /{n}`, `POST`,
`POST /{n}/anular`) — los SPs con su JSON de nombres resueltos y
renglones anidados. Nada nuevo en sesión.
